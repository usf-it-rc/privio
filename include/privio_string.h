/* 
 * Its hard to believe that after 40 odd years of C being in existence
 * that I had to implement this... but here goes
 */

/* search and replace... for fucking real? */
char *strsr(const char *, const char *, const char *);
int in_array(int *, int, int);
